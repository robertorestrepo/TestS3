{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
    <div class="col-md-12" >
        <a href="#" class="btn btn-success" > CREATE</a>
    </div>
{% endblock %}
